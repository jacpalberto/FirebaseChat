<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:id="@+id/rlItemRoom"
    android:background="@drawable/custom_rounded_layout_blue"
    android:padding="10dp">

    <TextView
        android:id="@+id/tvRoomTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textColor="@android:color/holo_blue_dark"
        android:textSize="17sp"
        tools:text="Sala 1" />

    <TextView
        android:id="@+id/tvDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvRoomTitle"
        android:padding="5dp"
        android:textSize="16sp"
        tools:text="Esta sala se usa para los desarrolladores del proyecto dbCinemex" />

    <TextView
        android:id="@+id/tvParticipants"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tvDescription"
        android:padding="5dp"
        android:textColor="@android:color/holo_red_dark"
        android:textSize="15sp"
        tools:text="Participantes: 12" />
</RelativeLayout>